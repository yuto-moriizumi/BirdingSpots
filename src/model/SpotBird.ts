import { Bird } from "./Bird";

export interface SpotBird extends Bird {
  JanFrequency: number[];
  FebFrequency: number[];
  MarFrequency: number[];
  AprFrequency: number[];
  MayFrequency: number[];
  JunFrequency: number[];
  JulFrequency: number[];
  AugFrequency: number[];
  SepFrequency: number[];
  OctFrequency: number[];
  NovFrequency: number[];
  DecFrequency: number[];
}
